> Recuperado do transcript da sessão 18-19/jul (retornos sobreviventes; sessão original morta por falso-positivo de Usage Policy 19/jul ~10h20). Conteúdo íntegro do agente.

---

# RELATÓRIO FINAL — mega-trabalho Rumo Pragas rodada 2 (18/jul/2026)

**Branch:** `mega-trabalho/pragas-2026-07` (4 commits próprios pré-existentes + 1 merge + 4 novos). Sem PR. Sem push.

**Alinhamento com main:** tinha 4 commits próprios à frente E 9 atrás (PR #52 grande + fix build Android). Merge com 4 conflitos reais resolvidos cirurgicamente — commit `640f9d7`. Gate ZERO-AE passou.

## Itens

**ITEM 1 — PR-26: `scripts/smoke-ai-chat.sh`** — commit `428c3d4` (175 linhas). Criei `scripts/` (não existia na raiz). 5 etapas contra jxcn: login REST → `ai-chat` compartilhado → `pragas_link_account` → `grant_pragas_ai_consent` (`2026-07-14.1`) → `ai-chat-pragas` (headers `X-Rumo-App: rumo-pragas` + `X-Pragas-AI-Consent-Version/Purpose` + `Idempotency-Key`). Contrato lido em `expo-app/services/ai-chat.ts:54,66-73`, `aiConsent.ts:6,60-63`, `pragasAccount.ts:60-91`. Credenciais 100% via env (`PRAGAS_SMOKE_EMAIL/PASSWORD/SUPABASE_URL/ANON_KEY`), `set -euo pipefail`, sem `set -x`, `ACCESS_TOKEN` extraído via `python3 os.environ` e scrubbed. Saída `PASS/FAIL + HTTP + latência` por etapa; exit 0 só se tudo 200. Gates: `bash -n` OK · `shellcheck 0.11.0` OK · não executado contra prod (sem env). Uuid via `python3 uuid.uuid4` com fallback `uuidgen`.

**ITEM 2 — PR-27: instrumentação `trackChatMessage`** — commit `806c8f4` (60 linhas). `expo-app/app/(tabs)/ai-chat.tsx:41,214` — adicionado import + chamada `trackChatMessage()` DEPOIS do `setMessages` do `aiMsg` (adoção = usuário recebeu resposta, não só apertou botão). Zero PII trafega — sanitizador de `services/analytics.ts` já bloqueia. Teste `expo-app/__tests__/app/ai-chat-analytics.contract.test.ts` (contrato source-level, mesmo padrão de `__tests__/lib/*`): 3 asserts — import exato, chamada dentro do handler `send` (bounded pelo próximo `useCallback`), ordem após `setMessages`. Render-based Testing-Library é caro demais pra 1 asserção. Gates: tsc 0 erros · lint dos arquivos tocados 0 · `jest ai-chat-analytics.contract` 3/3 · `jest analytics.test` 18/18 (sem regressão). Prettier reformatou o teste — mantido. Obs.: a função em `services/analytics.ts:221` chama-se `trackChatMessage`, não `trackChatMessageSent` (nome do evento é `chat_message_sent`) — usei a real.

**ITEM 3 — PR-18: cherry-pick `8e8515d`** — commit `adaeb47`. Show-stat mostrou 25 arquivos, TODOS em `supabase/migrations/` ou `supabase/migrations-proposals/` (0 mudanças de código de app/fn). Cherry-pick sem conflito. Reconciliação verbatim de 15 migrations já aplicadas em prod (md5 conferido) + 4 renames pra timestamps de prod + 1 movida pra `migrations-proposals/`. Não executa nada — só alinha arquivos ao histórico do jxcn.

**ITEM 4 — PR-19 (parcial): template assetlinks** — commit `93b7f14`. `expo-app/store-assets/assetlinks.template.json` (schema Google Digital Asset Links, `com.agrorumo.rumopragas`, relation `delegate_permission/common.handle_all_urls`, `sha256_cert_fingerprints: ["<PLAY_APP_SIGNING_SHA256>"]`) + `expo-app/store-assets/ASSETLINKS_README.md` (5 linhas: onde tirar SHA no Play Console → App signing → App signing key certificate; publicar em `https://pragas.agrorumo.com/.well-known/assetlinks.json` — deploy landing é gate CEO; validar via `curl` + Statement List API). JSON validado com `python3 json.load`. Fora de escopo: baixar SHA real (gate CEO — precisa acesso ao Play Console) e servir o `assetlinks.json` real.

**ITEM 5 — FCM (só investigação):** `firebase --version 15.15.0`, autenticado. Projeto `agrorumo` (599510455577). `firebase apps:list ANDROID` mostra **2 apps existentes** — "Rumo Máquinas (Android)" e **"Rumo Pragas IA Android"** (appId `1:599510455577:android:e80fa19b86b10e36cb6188`, package `com.agrorumo.rumopragas`). App JÁ existe — não criei nada. Rodei `firebase apps:sdkconfig ANDROID <appId> --out ~/.keys/pragas-google-services.json` (fora do repo, `~/.keys/` restrito). Package confirmado `com.agrorumo.rumopragas` (bate com o `app.json`). **NÃO commitei** o arquivo (google-services.json não é versionável — o `.gitignore` do expo-app já cobre `google-services.json`). Salvo em `/Users/manoelnascimento/.keys/pragas-google-services.json`. Próximo passo (fora do meu escopo): integrar no build via EAS secrets + plugin `@react-native-firebase`.

**ITEM 6 — evidência PR-20:** 3 greps confirmam que a classe "allowlist startsWith sem barra" **não existe no client Pragas** — (a) `grep -rn "startsWith"` em `expo-app/*.ts*` filtrado por `allowlist|origin|redirect`: **0 hits**; (b) URL builders com `SUPABASE_URL +`: **0 hits** no `services/`; (c) allowlists reais são `Set<string>.has()` (exact match) em `expo-app/api/mcp/server.ts:63,88,100` — não `startsWith`, então imune ao bug de subdomínio-shadow. `AppUpdate` valida por SCHEME allowlist (não URL). PR-20 procede como STALE (o Sync já sinalizou).

## Fora de escopo / gates CEO
- Deploy da landing `pragas.agrorumo.com` (ITEM 4): gate CEO.
- Integração `google-services.json` no build EAS (ITEM 5): próximo trabalho.
- Executar `smoke-ai-chat.sh` contra prod (ITEM 1): precisa criar conta demo dedicada + set envs.

Arquivos absolutos das mudanças:
- `/Users/manoelnascimento/AgroRumo Projetos/Apps/rumo-pragas/CLAUDE.md`
- `/Users/manoelnascimento/AgroRumo Projetos/Apps/rumo-pragas/expo-app/app.json`
- `/Users/manoelnascimento/AgroRumo Projetos/Apps/rumo-pragas/expo-app/services/analytics.ts`
- `/Users/manoelnascimento/AgroRumo Projetos/Apps/rumo-pragas/expo-app/__tests__/services/analytics.test.ts`
- `/Users/manoelnascimento/AgroRumo Projetos/Apps/rumo-pragas/scripts/smoke-ai-chat.sh`
- `/Users/manoelnascimento/AgroRumo Projetos/Apps/rumo-pragas/expo-app/app/(tabs)/ai-chat.tsx`
- `/Users/manoelnascimento/AgroRumo Projetos/Apps/rumo-pragas/expo-app/__tests__/app/ai-chat-analytics.contract.test.ts`
- `/Users/manoelnascimento/AgroRumo Projetos/Apps/rumo-pragas/expo-app/store-assets/assetlinks.template.json`
- `/Users/manoelnascimento/AgroRumo Projetos/Apps/rumo-pragas/expo-app/store-assets/ASSETLINKS_README.md`
- `/Users/manoelnascimento/.keys/pragas-google-services.json` (FCM, fora do repo)
