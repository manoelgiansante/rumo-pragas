> Recuperado do transcript da sessão 18-19/jul (retornos sobreviventes; sessão original morta por falso-positivo de Usage Policy 19/jul ~10h20). Conteúdo íntegro do agente.

# SYNC rodada 2 — Rumo Pragas (18/jul/2026) — read-only, nenhuma edição feita

Working tree confirmado intocado: `git status` → `?? supabase/migrations/20260713120000_paid_photo_quota.sql` (segue untracked, não mexi). HEAD `662477b` == `origin/main`.

1. **PR-16 AGRIO_LABEL_MAP — STALE-JÁ-FEITO (código); medição pendente.** `git grep agrio_label` → `supabase/functions/diagnose/agrio.ts:142` e `diagnose-pragas/agrio.ts:174` têm `AGRIO_LABEL_MAP` + telemetria `agrio_label_unmapped` via Sentry `logError` tags (`diagnose/agrio.ts:271`, `diagnose-pragas/agrio.ts:298`). Não há tabela — evento é só Sentry; não contei nesta sessão (não inventado). Prod: `diagnose v71` e `diagnose-pragas v1` ACTIVE. **Próxima ação:** 1 query Sentry (projeto rumo-pragas, tag `step:agrio_label_unmapped`) antes de gastar fix.

2. **PR-17 FCM Android — ABERTO-CONFIRMADO.** `find`/`git ls-files` → **zero** `google-services.json` (real ou placeholder); `app.json`: `android.googleServicesFile: None`, mas `expo-notifications` plugin (linha 234) + `POST_NOTIFICATIONS` (171) presentes. `pragas-send-push` tem fonte na main e está deployada **v22 ACTIVE**. A policy de build **já valida** o arquivo (`validateGoogleServicesConfiguration`, `native-signing-policy.mjs:877`, checa `policy.firebase.packageName/appId`). **Falta exatamente:** obter o `google-services.json` real do projeto Firebase (secret, fora do repo), apontá-lo em `android.googleServicesFile` no build, e rebuildar o binário. Server-side pronto.

3. **PR-18 migration history — ABERTO-CONFIRMADO na main (resolvido parcialmente em branch).** Local main: 28 tracked + 1 untracked. Por versão exata, só **4** batem com prod (`202607151700/1710/1720/1730`). As 2 do candidato (`20260714143000_pragas_backend_security`, `20260714150000_pragas_export_consistency`) **estão na main** (`git ls-files`) mas **NÃO aparecem** no history prod (`list_migrations`) → **não aplicadas**. Prod tem ~14 versões pragas sem arquivo na main (5× `pragas_audit_*` 21/mai, demo/reviewer accounts, `pragas_profiles_push_contract_align`, `pragas_images_storage_hardening`, `pragas_trial_14d_pro_and_asaas_pix`, `pragas_secdef_search_path_hardening` 17/jul, etc.). **MUDOU:** existe commit `8e8515d` "reconcilia arquivos de migrations com historico de prod jxcn" — mas só na branch `mega-trabalho/cross-pragas-staging-2026-07`, não na main. **Próxima ação:** avaliar cherry-pick do `8e8515d` pra main (docs-only, sem gate).

4. **PR-19 assetlinks — ABERTO-CONFIRMADO.** `curl` → **404** (página "Página não encontrada"). Worktree da landing (`../rumo-pragas-landing/public/`) não tem `.well-known/`. **Próxima ação:** criar `public/.well-known/assetlinks.json`; o SHA-256 do Play App Signing exige console (gate parcial).

5. **PR-20 allowlist startsWith — NÃO ENCONTRADO na main (provável STALE).** `git grep startsWith expo-app/` filtrado por store/url/link → só checks de **scheme** `url.startsWith('https://')` (`services/diagnosis.ts:96`, `pragasAccount.ts:66,99`, `userDataExport.ts:150`) — não é a classe "domínio sem barra". Nenhuma allowlist de store URL com prefixo frouxo localizada. **Próxima ação:** se o backlog tiver arquivo:linha original, re-checar pontual; senão fechar como stale.

6. **PR-21 sitemap — STALE-JÁ-FEITO.** `curl` → `308 -> https://pragas.agrorumo.com/sitemap-index.xml`, final **200**; `robots.txt` **200** com conteúdo válido. **Próxima ação:** nenhuma.

7. **PR-24 share/alertas — MUDOU (metade já existe).** Share de diagnóstico **JÁ EXISTE na main**: `app/diagnosis/result.tsx:274` `buildShareText` (texto WhatsApp-formatado), import `Share` (linha 10), tratamento do deep link `whatsapp://` com fix documentado RUMO-PRAGAS-5, `trackShareDiagnosis` (linha 50); prod tem 1 evento `share_diagnosis` (14/jul). Alertas por região **NÃO existem**: `home.regionalAlerts` é label do card de clima ("Local Climate Conditions"), e `STORE_LISTING.md:15` **proíbe** reintroduzir "regional maps/community reports" sem evidência+revisão legal. Infra aproveitável: `WeatherCard`+`services/weather`/`alerts` (testes existem), tabela legada `pragas_outbreaks` existe em prod com **0 rows**, push server pronto (item 2). **Próxima ação:** tratar só a metade "alertas" e validar contra o STORE_LISTING antes.

8. **PR-26 smoke script — ABERTO-CONFIRMADO.** `find . -name "smoke*"` → só `.maestro/smoke-test.yaml`. Não existe `scripts/smoke-ai-chat.sh` em lugar nenhum. **Próxima ação:** criar o script (item executável sem gate, credencial via env).

9. **PR-27 métrica de adoção chat — ABERTO-CONFIRMADO (meio-fio).** Client: `services/analytics.ts:203` define `trackChatMessageSent` mas `git grep trackChatMessageSent` → **zero callers**. Flush vai pra `supabase.functions.invoke('pragas-analytics')` (analytics.ts:166) → fn v1 ACTIVE grava via RPC `record_pragas_analytics_events` em `analytics_events`. Prod: 109 rows pragas/null, **nenhum** `chat_message_sent`; eventos reais recentes: `onboarding_started` (17/jul), `pest_detail_viewed`, `share_diagnosis`. `pragas_chat_messages` = 0 (cego por design, ZERO-V). **Próxima ação:** 1 linha — chamar `trackChatMessageSent()` no send do chat (binário novo necessário pra valer).

10. **RECONCILIAÇÃO main × codex — STALE (PR #52 trouxe tudo).** `main..codex` mostra 46 commits "+" no `git cherry`, MAS `git diff main codex` = **apenas 3 arquivos, 109 linhas**: `CLAUDE.md` (bloco docs), `expo-app/app.json` (pin b64/vc50), `expo-app/plugins/withKotlinInProcess.js` (fix Android local) — todos **da main, ausentes na codex** (main = codex + 3 melhorias). Zero conteúdo da codex fora da main. As 12 fns dedicadas **todas têm fonte na main** (`ls supabase/functions/` = 33 entradas, incl. `pragas-global-account-deletion`). **Próxima ação:** branch codex pode ser arquivada.

11. **PROD atual — MUDOU vs coverage 15/jul (deploy total já aconteceu).** `list_edge_functions` jxcn: **todos os slugs dedicados deployados ACTIVE** — `diagnose-pragas v1`, `ai-chat-pragas v1`, `pragas-analytics v1`, `pragas-delete-user-account v1`, `pragas-export-user-data v1`, `pragas-global-account-deletion v1`, `pragas-process-ai-idempotency v1`, `pragas-process-deletions v1`, `pragas-reactivate-account v1`, `report-ai-content v1`, `report-diagnosis-feedback v1`, `admin-ai-content-reports v1`, `pragas-send-push v22`; compartilhados: `ai-chat v54`, `diagnose v71`, `analytics v32`. SQL: `to_regclass('public.pragas_app_links')` → **existe** (também `pragas_outbreaks`, `pragas_chat_messages`, `analytics_events`). O item "9 fns não deployadas" do coverage está **obsoleto**.

12. **Versões de build — ABERTO com conflito confirmado no trilho EAS.** `app.json`: `version 1.0.11`, `ios.buildNumber "64"`, `android.versionCode 50`; `expo-app/eas.json`: `appVersionSource: "local"`. Policy: `latestStoreBuildNumber: 63` (iOS), `latestStoreVersionCode: 54` (Android) (`native-signing-policy.mjs:26,42`). O runner protegido (`native-local-production-build.mjs:659`) **ignora o pin**: deriva `buildVersion = commit_timestamp − 1577836800` (≈206,6M hoje) e `validateNativeBuildVersion` exige > baseline — passa nas 2 plataformas. **Mas** um `eas build --local` clássico usa o app.json: iOS b64>63 OK; **Android vc50 ≤ 54 = upload recusado pelo Play** (draft 1.0.8=54 já reservou o código). O pin `vc50` do `662477b` **conflita** com a exigência >54 — só é inofensivo se o build for pelo runner protegido. **Próxima ação:** decidir trilho canônico; se EAS clássico, rebumpar vc≥55.

13. **Cert iOS — BLOQUEADO-GATE (externo).** `expo-app/store-assets/APPLE_SIGNING_ROTATION_BLOCKER.md` existe na main; estado declarado 15/07: "**BLOQUEADO EXTERNAMENTE PARA BUILD E SUBMISSÃO iOS**" — cert Distribution + profile + senha exigem rotação por operador autorizado antes de qualquer IPA elegível. **Próxima ação:** nenhuma automatizável; gate CEO/operador.

**Executável sem gate HOJE:** (a) PR-26 criar `scripts/smoke-ai-chat.sh`; (b) PR-27 wire de `trackChatMessageSent` no chat (1 linha + teste); (c) PR-18 cherry-pick do `8e8515d` (reconciliação de migrations, docs-only) pra main; (d) PR-19 criar o arquivo `assetlinks.json` na landing (SHA-256 do console fica pendente); (e) fechar como STALE: PR-16 (código), PR-21, item 10 (arquivar branch codex) e provável PR-20. Bloqueados: iOS build/submit (item 13), FCM depende de secret Firebase (item 2), Android via EAS clássico depende de decisão vc≥55 (item 12).
