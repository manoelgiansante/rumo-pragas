> Recuperado do transcript da sessão 18-19/jul (retornos sobreviventes; sessão original morta por falso-positivo de Usage Policy 19/jul ~10h20). Conteúdo íntegro do agente.

## Verdade-terreno + refutação adversarial — gaps Rumo Pragas (18/jul, READ-ONLY, branch `mega-trabalho/pragas-2026-07`)

| # | Gap | Veredito | Evidência (arquivo:linha) | Nota |
|---|-----|----------|---------------------------|------|
| 1 | Alertas regionais de risco por push | **PARCIAL — versão local JÁ EXISTE; push servidor REFUTADO agora** | Regras de risco por clima: `expo-app/services/alerts.ts:27-182` (ferrugem/fúngicas/mofo/ácaros/cigarrinha + padrões multi-dia). Notificação **local** já dispara p/ severity high: `services/notifications.ts:375-425` (`scheduleClimateRiskNotifications`, cap 2/lote). Infra push pronta: tabela `pragas_push_tokens` (`supabase/migrations/20260715171000_pragas_prod_compat_runtime.sql:1879`) + fn `pragas-send-push` — **mas só aceita `target_user_ids` explícitos** (`supabase/functions/pragas-send-push/index.ts:39,125-131`), zero targeting geográfico. Localização consentida = arredondada a 2 casas (~1,1 km): `services/locationPrivacy.ts:18-23`. | Push servidor exigiria: coluna de região consentida + cron + fetch de clima por região + targeting novo na fn = migration + edge fn + cron. Esforço ≥1-2 dias, custo recorrente de API, e o valor de marketing ("alerta de risco na sua região") **já é entregue localmente**. Clima-derivado ≠ "community reports" (STORE_LISTING:15 proíbe community feed/regional user map — risco climático educacional passa), mas não é pro lançamento. |
| 2 | Lembrete de re-inspeção (notificação local) | **REAL-APROVADO** | Zero código hoje: `git grep reinspec\|reminder\|lembrete` em app/services/components = vazio. Infra 100% pronta: `scheduleNotificationAsync` (`notifications.ts:407`), canais Android, permission handling, `isPushNotificationsEnabled`. ⚠️ o helper climático **capa delay em 86.400s (24h)** (`notifications.ts:418-420`) — lembrete de 3-7 dias precisa de função dedicada sem esse cap. | Client-only, zero servidor. Botão na `result.tsx` pós-diagnóstico ("Lembrar de reinspecionar em 3/7 dias") → função nova `scheduleReinspectionReminder` + ~8 chaves i18n ×3. Retenção pura, zero risco de contrato. |
| 3 | Ponte AGROFIT/MAPA | **JÁ-EXISTE** | `components/MipCard.tsx:86-88,207-211` (botão AGROFIT com analytics); `app/diagnosis/pest/[id].tsx:157-158,529-532` (busca AGROFIT por praga); `app/terms.tsx:93` e `STORE_LISTING.md:16` consagram o link como estratégia de compliance. | Nada a fazer. Link pra base oficial **não** fere contrato — é a defesa contra parecer prescrição. Usar como copy de marketing. |
| 4 | Inimigos naturais / controle biológico | **JÁ-EXISTE** | Dados: `data/mip/*.ts` campo `biologico` (ex.: `algodao.ts:113-115,225,276` — Trichogramma, Beauveria, joaninhas, parasitoides). UI: `MipCard.tsx:176-179` renderiza `mip.biologicalActions`; result usa MipCard (`result.tsx:1150`); ficha usa `useMipKnowledge` + mapeia MIP (`pest/[id].tsx:48-90`). | Nada a fazer. Paridade com Guia InNat já embarcada — material de divulgação. |
| 5 | Biblioteca offline | **REFUTADO (já somos offline)** | `app/(tabs)/library.tsx:42` — `PESTS_BY_CROP` é **record hardcoded no bundle**; zero import de supabase/fetch no arquivo. `pestRegistry.ts` é só cache AsyncStorage de enrichment. | Vira **copy honesta**: "biblioteca e fichas MIP funcionam sem sinal; o diagnóstico exige internet" — respeita a proibição de "offline analysis" (STORE_LISTING:73). |
| 6 | Botão discordar/feedback | **JÁ-EXISTE** | `result.tsx:867-940` — card completo com radiogroup de veredito, alternativa, notas, submit (testIDs `diagnosis-feedback-*`), idempotência (`:566-574`); service `diagnosisFeedback.ts` + fn `report-diagnosis-feedback`. | Nada a fazer. |
| 7 | Janela de condições de aplicação | **REAL-APROVADO (2º nível, com guarda de copy)** | WeatherCard já mostra vento km/h, temp, UR, chuva (`WeatherCard.tsx:123`), mas `weather.ts` busca só `current=` — **sem hourly** (`weather.ts:241`; grep `hourly` = vazio). Nenhuma lógica de janela existe. | Client-only: adicionar `&hourly=wind_speed_10m,precipitation_probability,relative_humidity_2m` (Open-Meteo grátis) + card "condições climáticas p/ manejo nas próximas 24h" (favorável/desfavorável por vento/chuva/UR). SEM produto/dose/momento imperativo + disclaimer "consulte o responsável técnico" → condição climática genérica ≠ prescrição. ~12-15 strings ×3. Revisar copy contra STORE_LISTING:73 antes de mergear. |
| 8 | Comunidade/Q&A | **REFUTADO** | Tabelas legadas `pragas_community_posts/likes` existem só em migrations de RLS antigas (`20260521001318...sql:70-98`), 0 rows, zero UI. | STORE_LISTING:15 declara explicitamente que o app **não tem** community feed; UGC = Apple 1.2 (report/block/filter obrigatórios) + moderação = custo recorrente. Contra o contrato e o prazo. |
| 9 | Scouting/talhão georreferenciado | **REFUTADO** | grep `talhao\|scouting` = vazio. | Semanas de trabalho + reter geo de precisão **contradiz o design de privacidade** (`locationPrivacy.ts:9` — "without retaining field-level precision"). Não é launch-critical. |
| 10 | Calculadora fertilizante / contagem armadilha | **REFUTADO** | grep `fertiliz\|armadilha` = vazio. | Fertilizante = fora do escopo do produto (pragas); 1 concorrente cada = sinal fraco; contagem de armadilha beira protocolo de monitoramento/nível de controle (zona de prescrição). |

**Transversais:** (a) Fluxo de push token **existe e roda**: `useNotifications.ts:143` (`persistPushTokenToServer` force), `notifications.ts:249-253` (getExpoPushTokenAsync + save local), RPC `touch_pragas_push_token` (`notifications.ts:90`), tabela auditada no jxcn. Só falta *targeting* — a fn de envio é por user_ids. (b) i18n: gap típico = 8-15 chaves × 3 idiomas (24-45 entradas); lembrete ≈ 24 entradas — trivial. (c) Maior divulgação/menor risco: **não é gap novo — é empacotar o que já existe** (biblioteca offline + MIP biológico + ponte AGROFIT + alertas climáticos locais = 4 claims honestos prontos para copy); entre os novos, a janela de aplicação (#7) é o mais "screenshotável" (paridade Plantix), o lembrete (#2) é o de retenção.

## TOP 3 para HOJE (ordem)

1. **Lembrete de re-inspeção (#2)** — botão pós-diagnóstico em `result.tsx`, função dedicada de agendamento (sem o cap de 24h do helper climático), respeitando `isPushNotificationsEnabled`; ~8 chaves i18n ×3; client-only, zero servidor, zero risco legal.
2. **Copy honesta de lançamento (#5 + já-existentes)** — zero código de produto: store listing/onboarding destacando "biblioteca e MIP funcionam sem sinal (diagnóstico exige internet)", controle biológico embarcado e ponte AGROFIT oficial. Cuidado: mudança de STORE_LISTING = revisão contra a linha 73 (nada de "offline analysis").
3. **Janela de condições climáticas p/ manejo (#7)** — estender `weather.ts` para hourly + card 24h favorável/desfavorável por vento/chuva/UR com disclaimer não-prescritivo; ~12-15 chaves ×3. Só mergear após passar a copy pelo filtro STORE_LISTING/CONAR (condição climática genérica, nunca "pulverize agora" nem produto).

Gaps 3, 4 e 6 já estão implementados — a pesquisa de mercado subestimou o próprio app; gaps 1 (metade local), 8, 9 e 10 refutados por contrato, privacidade ou esforço desproporcional ao lançamento de 23/jul.
