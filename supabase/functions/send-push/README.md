# ⛔ NÃO DEPLOYAR ESTA FUNÇÃO

O slug `send-push` no projeto Supabase jxcn (shared) pertence ao **Rumo Confinamento** (lê `push_tokens`, tenant `vet_fazendas`) — deploy aqui faz **clobber** do app deles.

O Rumo Pragas usa o slug DEDICADO **`pragas-send-push`** (ver `../pragas-send-push/`). Este `index.ts` fica só como referência histórica.
